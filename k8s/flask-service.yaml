apiVersion: v1
kind: Service
metadata:
  name: flask-monitor-app
  namespace: monitoring
  labels:
    app: flask-monitor-app
spec:
  selector:
    app: flask-monitor-app
  ports:
  - name: web
    protocol: TCP
    port: 5000
    targetPort: 5000
  type: NodePort
